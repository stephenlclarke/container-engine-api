# Pull request 44: make container-engine-api SonarQube authoritative

## Summary

- Generate LLVM coverage from the complete Swift package test bundle plus the separately linked service executable, then convert it to SonarQube generic coverage XML.
- Enforce an 80% clean-gate floor and retain both LCOV and XML evidence without excluding maintained handoff or security code.
- Validate the project-level previous-version policy, identify every analysis by its exact commit SHA, and wait for the quality gate.
- Analyze trusted pull requests as pull requests and reject unresolved new issues or security hotspots in the previous-version period; fork and Dependabot pull requests skip the token-dependent job because repository secrets are unavailable to them.
- Exclude the generated Docker route ledger and document exact-file analyzer
  dispositions for fixed Docker/health paths and stable handoff/lifecycle
  protocol signatures.
- Accept the four existing concurrency/POSIX/NIO callback-bridge findings by
  exact SonarQube issue, keeping `swift:S3087` enabled in every source file.
- Explain intentionally empty protocol-drain and acknowledgement bodies in code.
- Document the RFC 6455 WebSocket accept-key SHA-1 transform as one narrow protocol exception.
- Honour the configured Swift toolchain for both the coverage build and test-binary discovery.

See [issue 43](https://github.com/stephenlclarke/container-engine-api/issues/43) and its [repository handoff](ISSUE-43.md).

## Validation

- [x] Coverage converter unit tests and Python compilation
- [x] GitHub Actions workflow validation
- [x] Markdown validation
- [x] Makefile dry-run validation
- [x] Xcode 26.6 instrumented package tests (186 tests passed)
- [x] Xcode 26.6 coverage: 84.54% (20,560 of 24,320 executable lines), above the 80% clean-gate floor
- [x] Full-history branch analysis uploaded the exact head with 62 maintained files; SonarQube Cloud then rejected persistent short-branch access on the current plan, so merged-main remains the authoritative full-history proof
- [ ] Exact-head SonarQube pull-request analysis
- [ ] Exact merged-main SonarQube analysis

Local execution of the Apple toolchain remains unavailable until the host's Xcode 27 licence is explicitly accepted. The authoritative workflow uses the repository's pinned Xcode 26.6 hosted runner. Raising overall coverage from 84.54% to the 90% improvement target requires roughly 1,330 additional meaningful covered lines, principally across handoff and security coordination; that debt remains visible rather than being hidden with broad exclusions.

## Compatibility and risk

No Engine API behavior changes. Generated route code is excluded by filename, while
every protocol/lifecycle disposition names one rule and one maintained file. The
security disposition is limited to `swift:S4790` in the dedicated
`WebSocketAcceptDigest.swift` helper because RFC 6455 fixes the accept-key
transform to SHA-1; every other cryptographic finding remains enabled.

Closes [#43](https://github.com/stephenlclarke/container-engine-api/issues/43).

Pull request: [#44](https://github.com/stephenlclarke/container-engine-api/pull/44)
